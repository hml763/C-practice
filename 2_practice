#include <stdio.h>

int main(){
	int j,num[3] = {0,0,0};
	float sum=0,ave=0;
	
	for(j=0;j<3;j++){
		printf("실수를 입력하시오 : ");
		scanf("%d",&num[j]); 
		sum = sum + num[j];
	}
	ave = sum/j;
	printf("sum : %f\nave : %f",sum,ave); 
}
